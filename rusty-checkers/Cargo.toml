[package]
name = "rusty-checkers"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
# this is to inidcator that will expost dynamic library
[lib]
crate-type = ["cdylib"]

[dependencies]
